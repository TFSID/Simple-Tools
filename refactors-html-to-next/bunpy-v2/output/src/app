import React from 'react';
// Import global styles, typically from src/styles/globals.css.
// In Next.js App Router, this is often handled by src/app/layout.tsx
// import '../styles/globals.css';

// You might import components from src/components like this:
// import MyButton from '@/components/MyButton'; // Assuming '@/' alias for 'src'

/**
 * Renders the default home page for the Next.js application.
 *
 * This component acts as the root page for the '/' route within the `src/app`
 * directory structure. It demonstrates a basic layout and includes
 * examples of content that would be rendered to HTML, implicitly using
 * global styles (e.g., Tailwind CSS classes) and referencing static assets
 * from the `public` directory.
 *
 * @returns {JSX.Element} The JSX for the home page.
 */
export default function Home(): JSX.Element {
  try {
    return (
      <main className="flex min-h-screen flex-col items-center justify-between p-24">
        {/*
          This section demonstrates a typical header/navigation bar area.
          It's styled with Tailwind CSS classes, assuming src/styles/globals.css
          or equivalent setup is configured to import Tailwind.
        */}
        <div className="z-10 w-full max-w-5xl items-center justify-between font-mono text-sm lg:flex">
          <p className="fixed left-0 top-0 flex w-full justify-center border-b border-gray-300 bg-gradient-to-b from-zinc-200 pb-6 pt-8 backdrop-blur-2xl dark:border-neutral-800 dark:bg-zinc-800/30 dark:from-inherit lg:static lg:w-auto lg:rounded-xl lg:border lg:bg-gray-200 lg:p-4 lg:dark:bg-zinc-800/30">
            Get started by editing&nbsp;
            <code className="font-mono font-bold">src/app/page.tsx</code>
          </p>
          <div className="fixed bottom-0 left-0 flex h-48 w-full items-end justify-center bg-gradient-to-t from-white via-white dark:from-black dark:via-black lg:static lg:size-auto lg:bg-none">
            {/* Example of referencing a static asset from the `public` directory */}
            <a
              className="pointer-events-none flex place-items-center gap-2 p-8 lg:pointer-events-auto lg:p-0"
              href="https://vercel.com?utm_source=create-next-app&utm_medium=appdir-template&utm_campaign=create-next-app"
              target="_blank"
              rel="noopener noreferrer"
            >
              By{' '}
              <img
                src="/vercel.svg" // Assumes 'vercel.svg' exists in the 'public' directory
                alt="Vercel Logo"
                className="dark:invert"
                width={100}
                height={24}
                priority
              />
            </a>
          </div>
        </div>

        {/* Main content area or hero section */}
        <div className="relative z-[-1] flex place-items-center before:absolute before:h-[300px] before:w-full before:-translate-x-1/2 before:rounded-full before:bg-gradient-radial before:from-white before:to-transparent before:blur-2xl before:content-[''] after:absolute after:-z-20 after:h-[180px] after:w-full after:translate-x-1/3 after:bg-gradient-conic after:from-sky-200 after:via-blue-200 after:blur-2xl after:content-[''] before:dark:bg-gradient-to-br before:dark:from-transparent before:dark:to-blue-700 before:dark:opacity-10 after:dark:from-sky-900 after:dark:via-[#0141ff] after:dark:opacity-40 sm:before:w-[480px] sm:after:w-[240px]">
          {/* Another example of a static asset */}
          <img
            className="relative dark:drop-shadow-[0_0_0.3rem_#ffffff70] dark:invert"
            src="/next.svg" // Assumes 'next.svg' exists in the 'public' directory
            alt="Next.js Logo"
            width={180}
            height={37}
            priority
          />
        </div>

        {/* Grid of informational links or features */}
        <div className="mb-32 grid text-center lg:mb-0 lg:w-full lg:max-w-5xl lg:grid-cols-4 lg:text-left">
          {/* Example link to documentation */}
          <a
            href="https://nextjs.org/docs?utm_source=create-next-app&utm_medium=appdir-template&utm_campaign=create-next-app"
            className="group rounded-lg border border-transparent px-5 py-4 transition-colors hover:border-gray-300 hover:bg-gray-100 hover:dark:border-neutral-700 hover:dark:bg-neutral-800/30"
            target="_blank"
            rel="noopener noreferrer"
          >
            <h2 className="mb-3 text-2xl font-semibold">
              Docs{' '}
              <span className="inline-block transition-transform group-hover:translate-x-1 motion-reduce:transform-none">
                -&gt;
              </span>
            </h2>
            <p className="m-0 max-w-[30ch] text-sm opacity-50">
              Find in-depth information about Next.js features and API.
            </p>
          </a>

          {/* Additional links can be added here, mimicking a typical Next.js starter */}
          {/*
          <MyButton text="Click Me" onClick={() => console.log("Button clicked")} />
          */}
        </div>
      </main>
    );
  } catch (error) {
    // Basic error logging for component rendering issues.
    // In a real application, you might use an Error Boundary component for better UX.
    console.error('Error rendering Home component:', error);
    // You could return a fallback UI here.
    return (
      <div className="flex min-h-screen items-center justify-center text-red-500">
        <h1>Oops! Something went wrong.</h1>
        <p>Please try again later.</p>
      </div>
    );
  }
}